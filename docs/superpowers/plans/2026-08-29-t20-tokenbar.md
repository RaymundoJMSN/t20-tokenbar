# t20-tokenbar Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Módulo Foundry `t20-tokenbar` que patcha o Monk's TokenBar pra Tormenta20: level up real, inspiração do `t20-inspiracao`, stats separados mestre × jogadores com texto do healthEstimate, e modificadores no diálogo de teste.

**Architecture:** Um `main.mjs` que importa as classes do monks-tokenbar pela mesma URL que o Foundry usa (mesma instância de módulo ES), faz 2 overrides de protótipo (`getValue`, `assignXP`) e o resto via hooks públicos (`renderTokenBar`, `getTokenbarContextOptionsTokenBar`, `updateActor`, `renderChatMessageHTML`). Regras puras de nível em `regras.mjs` com self-test node.

**Tech Stack:** Foundry VTT v13 (ApplicationV2/DialogV2), sistema tormenta20, JS puro sem build, sem dependência npm.

**Spec:** `docs/superpowers/specs/2026-08-29-t20-tokenbar-design.md` — lê ele primeiro se algo aqui parecer ambíguo.

## Global Constraints

- Foundry v13; sistema `tormenta20`; strings de UI em pt-BR.
- Sem build, sem npm, sem lib externa. O módulo roda direto da pasta.
- Repo/dev: `C:\Users\rayna\Soltos\t20-tokenbar`. Foundry carrega via junction `X:\FoundryVTT\Data\modules\t20-tokenbar` → essa pasta. Editar e F5 no Foundry (CSS tem hotReload).
- Só 2 patches no monks: `Tormenta20Rolls.prototype.getValue` e `.assignXP`. Todo o resto é hook público ou subclasse. NÃO tocar em arquivo dentro de `X:\FoundryVTT\Data\modules\monks-tokenbar`.
- `healthEstimate` e `t20-inspiracao` são opcionais: todo acesso é guardado por `game.healthEstimate`/`game.modules.get("t20-inspiracao")?.active`. Só `monks-tokenbar` é dependência dura (`requires` no module.json).
- Depois de cada edição em `.mjs`: `node --check <arquivo>` (só parse; imports do Foundry não resolvem fora dele e não precisam).
- Git local, sem GitHub. Commits com `-c user.name="Ray" -c user.email="raimundojmdsn@gmail.com"` e trailer `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- Verificação dentro do Foundry é manual e fica toda na Task 8 (o executor não tem como rodar o Foundry).
- Fatos do core/monks já verificados no fonte — não re-derivar: hook de render AppV2 recebe `(app, element, context, options)` com `element` = HTMLElement (`application.mjs:524`); o hook do menu de contexto da barra chama `getTokenbarContextOptionsTokenBar` com `(app, menuItems)` e `li` HTMLElement (`apps/tokenbar.js:294` + `application.mjs:1226`); `DialogV2.prompt` tem `rejectClose: false` por padrão (dismiss resolve `null`); `renderChatMessageHTML` recebe `(message, html)` com `html` HTMLElement.

---

### Task 1: Esqueleto do módulo + junction

**Files:**
- Create: `module.json`
- Create: `scripts/main.mjs`
- Create: `styles/t20-tokenbar.css`
- Create: junction `X:\FoundryVTT\Data\modules\t20-tokenbar`

**Interfaces:**
- Produces: constante `MOD = "t20-tokenbar"`, helper `temInspiracao()`, imports `MonksTokenBar`, `TokenBar`, `EditStats`, `Tormenta20Rolls`, `podeSubir`, `classesDe` — tudo no topo de `scripts/main.mjs`; as tasks seguintes só **acrescentam** código nesse arquivo, nunca mexem no topo.

- [ ] **Step 1: Criar `module.json`**

```json
{
  "id": "t20-tokenbar",
  "title": "T20 TokenBar",
  "description": "Compatibilidade total do Monk's TokenBar com Tormenta20: subir de nível de verdade, inspiração do t20-inspiracao, stats separados para mestre e jogadores com o texto do Health Estimate, e modificadores no diálogo de teste.",
  "version": "1.0.0",
  "compatibility": { "minimum": "13", "verified": "13" },
  "relationships": {
    "requires": [{ "id": "monks-tokenbar", "type": "module" }],
    "systems": [{ "id": "tormenta20", "type": "system" }]
  },
  "esmodules": ["scripts/main.mjs"],
  "styles": ["styles/t20-tokenbar.css"],
  "flags": {
    "hotReload": { "extensions": ["css"], "paths": ["styles/t20-tokenbar.css"] }
  },
  "authors": [{ "name": "Ray" }]
}
```

- [ ] **Step 2: Criar `scripts/main.mjs` (esqueleto)**

O import relativo `../../monks-tokenbar/...` resolve pra mesma URL que o Foundry usa ao carregar o monks — o registry de módulos ES deduplica, então são as MESMAS classes/instâncias, não cópias. `regras.mjs` ainda não existe; o import fica aqui desde já pra nenhuma task futura precisar editar o topo do arquivo (o `node --check` só parseia, não resolve).

```js
// t20-tokenbar — mod do Monk's TokenBar para Tormenta20.
// 2 patches de protótipo (getValue/assignXP do Tormenta20Rolls); o resto é hook público.
// Spec: docs/superpowers/specs/2026-08-29-t20-tokenbar-design.md
import { MonksTokenBar } from "../../monks-tokenbar/monks-tokenbar.js";
import { TokenBar } from "../../monks-tokenbar/apps/tokenbar.js";
import { EditStats } from "../../monks-tokenbar/apps/editstats.js";
import { Tormenta20Rolls } from "../../monks-tokenbar/systems/tormenta20-rolls.js";
import { podeSubir, classesDe } from "./regras.mjs";

const MOD = "t20-tokenbar";
const temInspiracao = () => game.modules.get("t20-inspiracao")?.active;

Hooks.once("init", () => {
  console.log(`${MOD} | init`);
});
```

- [ ] **Step 3: Criar `styles/t20-tokenbar.css`**

```css
/* t20-tokenbar — badge de inspiração e card de nível. Regras entram nas tasks 6 e 7. */
```

- [ ] **Step 4: Verificar sintaxe**

Run: `node --check "C:/Users/rayna/Soltos/t20-tokenbar/scripts/main.mjs"`
Expected: sem saída, exit 0.

- [ ] **Step 5: Criar a junction (PowerShell)**

```powershell
New-Item -ItemType Junction -Path "X:\FoundryVTT\Data\modules\t20-tokenbar" -Target "C:\Users\rayna\Soltos\t20-tokenbar"
```

Expected: imprime o item criado. Confirmar com `Get-Item "X:\FoundryVTT\Data\modules\t20-tokenbar\module.json"`.

- [ ] **Step 6: Commit**

```bash
cd "C:/Users/rayna/Soltos/t20-tokenbar" && git add module.json scripts/main.mjs styles/t20-tokenbar.css && git -c user.name="Ray" -c user.email="raimundojmdsn@gmail.com" commit -m "feat: esqueleto do modulo + junction no Foundry

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: `regras.mjs` — regras puras de nível (TDD)

**Files:**
- Create: `scripts/regras.mjs` (o self-test `--check` é o teste; sem framework, padrão do t20-inspiracao)

**Interfaces:**
- Produces: `NIVEL_MAX = 20`; `podeSubir(nivel, xp, proximo) → boolean`; `classesDe(items) → [{ id, nome, niveis }]` (aceita qualquer iterável de itens; filtra `type === "classe"`; `niveis` numérico com default 1, igual ao getter `nivel` do sistema).
- Consumed by: Task 7 (`subirNivel`/`verificarNivel`).

- [ ] **Step 1: Escrever o arquivo com os asserts e stubs falhos**

Stubs devolvem o oposto de propósito — prova que o `--check` pega regressão.

```js
// Regras puras de subir de nível — sem Foundry. Teste: node scripts/regras.mjs --check
export const NIVEL_MAX = 20;

export function podeSubir(nivel, xp, proximo) {
  return false; // stub
}

export function classesDe(items) {
  return []; // stub
}

// Self-test (node só; no browser process não existe)
if (typeof process !== "undefined" && process.argv?.includes("--check")) {
  const assert = (cond, msg) => {
    if (!cond) { console.error("FALHOU:", msg); process.exit(1); }
  };

  // podeSubir — o teto de 20 é obrigatório: getLevelExp congela xp.proximo em
  // 190000 no nível 20 (tormenta20.mjs:7902), senão a condição fica verdadeira pra sempre.
  assert(podeSubir(1, 1000, 1000), "nível 1 com 1000/1000 sobe");
  assert(!podeSubir(1, 999, 1000), "999/1000 não sobe");
  assert(podeSubir(19, 190000, 190000), "19→20 sobe");
  assert(!podeSubir(20, 999999, 190000), "nível 20 nunca sobe (teto)");
  assert(!podeSubir(3, 5000, 0), "proximo 0 → ator sem XP configurado, não sobe");
  assert(!podeSubir(3, 5000, undefined), "proximo indefinido → não sobe");
  assert(!podeSubir(3, 5000, NaN), "proximo NaN → não sobe");

  // classesDe
  const cls = classesDe([
    { id: "a", type: "classe", name: "Guerreiro", system: { niveis: "3" } },
    { id: "b", type: "poder", name: "Ataque Extra", system: {} },
    { id: "c", type: "classe", name: "Ladino", system: {} },
  ]);
  assert(cls.length === 2, "só itens type classe entram");
  assert(cls[0].id === "a" && cls[0].nome === "Guerreiro" && cls[0].niveis === 3,
    "niveis vira número");
  assert(cls[1].niveis === 1, "niveis ausente → default 1 (como o getter nivel do sistema)");
  assert(classesDe([]).length === 0, "lista vazia → vazia");

  console.log("regras.mjs OK");
}
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `node "C:/Users/rayna/Soltos/t20-tokenbar/scripts/regras.mjs" --check`
Expected: `FALHOU: nível 1 com 1000/1000 sobe`, exit 1.

- [ ] **Step 3: Implementar de verdade**

Substituir os dois stubs por:

```js
export function podeSubir(nivel, xp, proximo) {
  nivel = Number(nivel); xp = Number(xp); proximo = Number(proximo);
  return Number.isFinite(proximo) && proximo > 0 && nivel < NIVEL_MAX && xp >= proximo;
}

export function classesDe(items) {
  return [...items]
    .filter((i) => i?.type === "classe")
    .map((i) => ({ id: i.id, nome: i.name, niveis: parseInt(i.system?.niveis) || 1 }));
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `node "C:/Users/rayna/Soltos/t20-tokenbar/scripts/regras.mjs" --check`
Expected: `regras.mjs OK`, exit 0.

- [ ] **Step 5: Commit**

```bash
cd "C:/Users/rayna/Soltos/t20-tokenbar" && git add scripts/regras.mjs && git -c user.name="Ray" -c user.email="raimundojmdsn@gmail.com" commit -m "feat: regras puras de nivel (podeSubir/classesDe) com self-test

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Overrides `getValue` e `assignXP`

**Files:**
- Modify: `scripts/main.mjs` (acrescentar no FIM do arquivo)

**Interfaces:**
- Consumes: `Tormenta20Rolls` (import da Task 1).
- Produces: nada consumido por outras tasks (o card de nível da Task 7 é disparado por `updateActor`, não por `assignXP`).

- [ ] **Step 1: Acrescentar os overrides no fim de `scripts/main.mjs`**

Override de protótipo funciona mesmo com a instância `MonksTokenBar.system` já criada — lookup de método é dinâmico. Patch no escopo do módulo (fora de hook) pra valer o mais cedo possível.

```js
// ---- Modificador no diálogo de teste (savingthrow.js:739 / contestedroll.js:401) ----
// "save" e "skill" são os dois perícias no T20 (Fort/Refl/Vont ficam em system.pericias,
// mesmo rollPericia). pericias[k].value é o total já derivado.
Tormenta20Rolls.prototype.getValue = function (actor, type, key) {
  const src = type === "ability" ? actor?.system?.atributos : actor?.system?.pericias;
  return src?.[key]?.value ?? null;
};

// ---- assignXP: só soma XP. O sussurro original do monks morre aqui de propósito —
// o card real de subir de nível é do updateActor (qualquer origem de XP), não só do monks.
Tormenta20Rolls.prototype.assignXP = async function (msgactor) {
  const actor = game.actors.get(msgactor.id);
  if (!actor) return;
  await actor.update({
    "system.attributes.nivel.xp.value":
      parseInt(actor.system.attributes.nivel.xp.value) + parseInt(msgactor.xp),
  });
};
```

- [ ] **Step 2: Verificar sintaxe**

Run: `node --check "C:/Users/rayna/Soltos/t20-tokenbar/scripts/main.mjs"`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/rayna/Soltos/t20-tokenbar" && git add scripts/main.mjs && git -c user.name="Ray" -c user.email="raimundojmdsn@gmail.com" commit -m "feat: getValue com modificadores T20 e assignXP sem sussurro morto

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: Settings + editor "Stats dos jogadores"

**Files:**
- Modify: `scripts/main.mjs` (acrescentar no fim + trocar o corpo do `Hooks.once("init")`)

**Interfaces:**
- Consumes: `EditStats`, `MonksTokenBar`, `MOD` (Task 1).
- Produces: settings `t20-tokenbar.stats-jogador` (world, Array de `{id, stat, icon, color}` — mesmo formato do setting `stats` do monks) e `t20-tokenbar.barra-estimativa` (world, Boolean, default true). Task 5 lê os dois.

- [ ] **Step 1: Acrescentar a subclasse no fim de `scripts/main.mjs`**

Por que cada override existe:
- construtor passa `{}` (não `null`) pro pai — `onSubmitForm`/`resetDefaults` do monks fazem `Object.keys(this.object)`, que explode com null/undefined;
- `onSubmitForm` do pai gravaria em `monks-tokenbar.stats` (a lista do MESTRE) quando o objeto é vazio — o nosso grava no nosso setting;
- `resetDefaults` do pai tem o mesmo problema; aqui reset = lista vazia = jogador volta a ver a lista do mestre;
- `get title()` — o pai tem getter que ignora `window.title` das options.

```js
// ---- Editor da lista de stats dos jogadores (reusa o EditStats do monks inteiro) ----
class EditStatsJogador extends EditStats {
  constructor(object, options = {}) {
    super({}, options);
    const salvos = game.settings.get(MOD, "stats-jogador");
    this.stats = (Array.isArray(salvos) ? foundry.utils.duplicate(salvos) : [])
      .map((s) => ({ ...s, id: s.id || foundry.utils.randomID() }));
    this.attributes = [...(this.attributes ?? []), "@estimate"];
  }

  static DEFAULT_OPTIONS = {
    id: "editstats-jogador",
    actions: { resetDefault: EditStatsJogador.resetDefaults },
    form: { handler: EditStatsJogador.onSubmitForm, closeOnSubmit: true },
  };

  get title() {
    return "Stats dos jogadores";
  }

  static onSubmitForm() {
    game.settings.set(MOD, "stats-jogador", this.stats);
    MonksTokenBar.tokenbar?.refresh();
    this.submitting = true;
  }

  // ponytail: reset = lista vazia → jogadores voltam a ver a lista do mestre
  static resetDefaults() {
    this.stats = [];
    this.render(true);
  }
}
```

- [ ] **Step 2: Trocar o corpo do `Hooks.once("init")`**

De:

```js
Hooks.once("init", () => {
  console.log(`${MOD} | init`);
});
```

Para:

```js
Hooks.once("init", () => {
  console.log(`${MOD} | init`);

  game.settings.register(MOD, "stats-jogador", {
    scope: "world",
    config: false,
    type: Object,
    default: [],
  });

  game.settings.register(MOD, "barra-estimativa", {
    name: "Barra de PV vira estimativa (jogadores)",
    hint: "Para quem não é mestre, a barra 1 dos tokens na tokenbar usa a cor e a faixa do Health Estimate em vez da fração exata de PV.",
    scope: "world",
    config: true,
    type: Boolean,
    default: true,
  });

  game.settings.registerMenu(MOD, "editStatsJogador", {
    name: "Stats dos jogadores",
    label: "Editar stats dos jogadores",
    hint: "Lista de stats que quem não é mestre vê na tokenbar. Vazia = a mesma lista do mestre. Aceita @estimate (texto do Health Estimate).",
    icon: "fas fa-users",
    restricted: true,
    type: EditStatsJogador,
  });
});
```

Nota: `EditStatsJogador` é referenciada dentro do `init` mas declarada depois no arquivo — `class` não hoisteia, porém o hook só EXECUTA no init do Foundry, muito depois do módulo inteiro avaliar. Sem problema.

- [ ] **Step 3: Verificar sintaxe**

Run: `node --check "C:/Users/rayna/Soltos/t20-tokenbar/scripts/main.mjs"`
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/rayna/Soltos/t20-tokenbar" && git add scripts/main.mjs && git -c user.name="Ray" -c user.email="raimundojmdsn@gmail.com" commit -m "feat: settings + editor de stats dos jogadores (subclasse do EditStats)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: Stats do jogador na barra + `@estimate` + barra pintada

**Files:**
- Modify: `scripts/main.mjs` (acrescentar no fim)

**Interfaces:**
- Consumes: `TokenBar.processStat` (estático do monks), settings da Task 4, `game.healthEstimate` (opcional).
- Produces: `estimativa(tokenDoc) → { desc, color, stroke, valor } | null` e `esc(s) → string` — a Task 7 usa `esc`.

- [ ] **Step 1: Acrescentar no fim de `scripts/main.mjs`**

Por que reescrever o DOM no render em vez de patchar o pipeline: `updateEntry` lê
`actor.getFlag('monks-tokenbar','stats')` ANTES do fallback global (`apps/tokenbar.js:564`) —
ator com stats por ficha vazaria a lista do mestre (PV exato) pro jogador por qualquer patch de
getter. Reescrever depois do render fecha esse buraco e evita o `TypeError` latente da linha 577.

```js
// ---- Estimativa do healthEstimate ----
// null = módulo ausente, token fora do canvas, sem PV numérico, ou escondido
// pelas regras do próprio healthEstimate (espelha _handleOverlay, healthEstimate.js:3783).
function estimativa(tokenDoc) {
  const he = game.healthEstimate;
  const t = tokenDoc?.object; // getEstimation exige o placeable, não o TokenDocument
  if (!he || !t?.actor) return null;
  try {
    if (he.breakOverlayRender(t)) return null;
    if (!game.user.isGM && he.hideEstimate(t)) return null;
    const { desc, color, stroke } = he.getEstimation(t);
    if (desc === undefined || desc === "") return null;
    const fraction = Number(he.getFraction(t));
    const { estimate } = he.getStage(t, fraction) ?? {};
    return { desc, color, stroke, valor: estimate?.value ?? Math.round(fraction * 100) };
  } catch {
    return null; // getFraction lança pra token sem PV
  }
}

const esc = (s) => Handlebars.escapeExpression(String(s ?? ""));

// Mesmo markup do token-list.hbs do monks, pro CSS dele valer igual.
function statHTML(stat, valor, cor) {
  const escondido = valor == null && !game.settings.get("monks-tokenbar", "show-undefined");
  const style = `${escondido ? "visibility:hidden;" : ""}color:${cor || "#f0f0f0"}`;
  const icone = stat.icon ? `<i class="fas ${esc(stat.icon)}"></i>` : "";
  return `<div class="token-stat flexrow" data-stat="${esc(stat.stat)}" style="${style}">${icone}<span>${esc(valor ?? "")}</span></div>`;
}

// ---- Stats do jogador: reescreve .token-stats de cada token depois do render ----
Hooks.on("renderTokenBar", (app, element) => {
  if (game.user.isGM) return;
  const lista = game.settings.get(MOD, "stats-jogador");
  const temLista = Array.isArray(lista) && lista.some((s) => s.stat);
  const pintarBarra = game.settings.get(MOD, "barra-estimativa");

  for (const li of element.querySelectorAll("li.token")) {
    const id = li.dataset.tokenId || li.dataset.actorId;
    const entry = app.entries.find((e) => e.token?.id === id || e.actor?.id === id);
    if (!entry?.actor) continue;
    const est = estimativa(entry.token);

    if (temLista) {
      const bloco = li.querySelector(".token-stats");
      if (bloco) {
        bloco.innerHTML = lista
          .filter((s) => s.stat)
          .map((s) => {
            if (s.stat === "@estimate") return statHTML(s, est?.desc ?? null, est?.color);
            const v =
              TokenBar.processStat(s.stat, entry.actor.system) ||
              TokenBar.processStat(s.stat, entry.token);
            return statHTML(s, v, s.color);
          })
          .join("");
      }
    }

    // Barra 1 vira a estimativa: cor E largura pela faixa — largura com a fração
    // real continuaria vazando o PV que o texto acabou de esconder.
    if (pintarBarra && est && entry.token?.getBarAttribute("bar1")?.attribute === "attributes.pv") {
      const barra = li.querySelector('.resource[resource="1"] .bar');
      if (barra) {
        barra.style.backgroundColor = est.color;
        barra.style.width = `${Math.clamp(est.valor, 0, 100)}%`;
      }
    }
  }
});

// O monks só re-renderiza pelo diff da lista DELE; PV pode nem estar nela.
Hooks.on("updateActor", (actor, changes) => {
  if (foundry.utils.hasProperty(changes, "system.attributes.pv"))
    MonksTokenBar.tokenbar?.render();
});
```

- [ ] **Step 2: Verificar sintaxe**

Run: `node --check "C:/Users/rayna/Soltos/t20-tokenbar/scripts/main.mjs"`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
cd "C:/Users/rayna/Soltos/t20-tokenbar" && git add scripts/main.mjs && git -c user.name="Ray" -c user.email="raimundojmdsn@gmail.com" commit -m "feat: stats do jogador com @estimate e barra pintada pela estimativa

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: Inspiração ✨N + menu de contexto + CSS

**Files:**
- Modify: `scripts/main.mjs` (acrescentar no fim)
- Modify: `styles/t20-tokenbar.css` (acrescentar)

**Interfaces:**
- Consumes: `temInspiracao()` (Task 1). Settings de terceiros: `monks-tokenbar.show-inspiration` (client), `t20-inspiracao.capMax`, flag `t20-inspiracao.pontos` no ator.
- Produces: nada pra outras tasks.

- [ ] **Step 1: Acrescentar no fim de `scripts/main.mjs`**

O `.inspiration-icon` já existe no template do monks pra todo token; o CSS dele mostra só quando
`inspiration="true"` (`css/tokenbar.css:411`). Pra T20 o atributo nunca liga (é o booleano do
dnd5e), então este hook assume o elemento. Roda pra TODO usuário (mestre também vê ✨N).

```js
// ---- Inspiração ✨N (t20-inspiracao) na caixinha de coroa do monks ----
Hooks.on("renderTokenBar", (app, element) => {
  if (!temInspiracao() || !game.settings.get("monks-tokenbar", "show-inspiration")) return;
  const cap = game.settings.get("t20-inspiracao", "capMax");
  for (const li of element.querySelectorAll("li.token")) {
    const id = li.dataset.tokenId || li.dataset.actorId;
    const entry = app.entries.find((e) => e.token?.id === id || e.actor?.id === id);
    const icone = li.querySelector(".inspiration-icon");
    if (!entry?.actor || !icone) continue;
    const pontos = Number(entry.actor.getFlag("t20-inspiracao", "pontos")) || 0;
    icone.setAttribute("inspiration", pontos > 0 ? "true" : "");
    icone.innerHTML =
      `<i class="fas fa-crown"></i>` +
      (pontos > 0 ? `<span class="t20tb-n">${pontos}</span>` : "");
    icone.dataset.tooltip = `${esc(entry.actor.name)}: ${pontos}/${cap} inspirações`;
  }
});

Hooks.on("updateActor", (actor, changes) => {
  if (foundry.utils.hasProperty(changes, "flags.t20-inspiracao.pontos"))
    MonksTokenBar.tokenbar?.render();
});

// Menu de contexto (hook público do monks — apps/tokenbar.js:294; li é HTMLElement)
Hooks.on("getTokenbarContextOptionsTokenBar", (app, options) => {
  if (!temInspiracao()) return;
  const atorDe = (li) => {
    const id = li.dataset.tokenId || li.dataset.actorId;
    return app.entries.find((e) => e.token?.id === id || e.actor?.id === id)?.actor;
  };
  const mudar = (li, delta) => {
    const a = atorDe(li);
    if (!a) return;
    const cap = game.settings.get("t20-inspiracao", "capMax");
    const p = Math.clamp((Number(a.getFlag("t20-inspiracao", "pontos")) || 0) + delta, 0, cap);
    a.setFlag("t20-inspiracao", "pontos", p);
  };
  const soGMPersonagem = (li) => game.user.isGM && atorDe(li)?.type === "character";
  options.push(
    {
      name: "Dar inspiração ✨",
      icon: '<i class="fas fa-crown"></i>',
      condition: soGMPersonagem,
      callback: (li) => mudar(li, 1),
    },
    {
      name: "Tirar inspiração",
      icon: '<i class="fas fa-crown"></i>',
      condition: soGMPersonagem,
      callback: (li) => mudar(li, -1),
    }
  );
});
```

- [ ] **Step 2: Acrescentar em `styles/t20-tokenbar.css`**

```css
/* A caixinha do monks tem 20px fixos; o número precisa caber (ancorada à direita, cresce pra esquerda) */
#tokenbar .token .inspiration-icon {
  width: auto;
  min-width: 20px;
  padding: 0 4px;
}
#tokenbar .token .inspiration-icon .t20tb-n {
  margin-left: 3px;
  font-weight: bold;
  font-size: 11px;
}
```

- [ ] **Step 3: Verificar sintaxe**

Run: `node --check "C:/Users/rayna/Soltos/t20-tokenbar/scripts/main.mjs"`
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/rayna/Soltos/t20-tokenbar" && git add scripts/main.mjs styles/t20-tokenbar.css && git -c user.name="Ray" -c user.email="raimundojmdsn@gmail.com" commit -m "feat: inspiracao do t20-inspiracao na barra + menu de contexto do mestre

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: Subir de nível de verdade

**Files:**
- Modify: `scripts/main.mjs` (acrescentar no fim)
- Modify: `styles/t20-tokenbar.css` (acrescentar)

**Interfaces:**
- Consumes: `podeSubir`, `classesDe` (Task 2), `esc` (Task 5), `MOD` (Task 1). Setting de terceiro: `monks-tokenbar.send-levelup-whisper` (world, `settings.js:325`).
- Produces: nada pra outras tasks.

Contexto obrigatório: nível no T20 é DERIVADO — `prepareBaseData` soma `system.niveis` dos itens
`classe` (tormenta20.mjs:17046). Subir = `+1` no item; escrever `nivel.value` não persiste. Estado
"resolvido" do card é derivado (`nivel.value >= nivelAlvo`), nunca gravado — o card é do GM e
jogador não pode atualizar mensagem alheia.

- [ ] **Step 1: Acrescentar no fim de `scripts/main.mjs`**

```js
// ---- Subir de nível de verdade ----
// Card criado só pelo GM ativo, pra qualquer origem de XP (monks, ficha, macro).
async function verificarNivel(actor) {
  if (game.users.activeGM?.id !== game.user.id) return;
  if (actor.type !== "character") return;
  if (!game.settings.get("monks-tokenbar", "send-levelup-whisper")) return;
  const n = actor.system.attributes.nivel;
  if (!podeSubir(n.value, n.xp.value, n.xp.proximo)) return;
  const nivelAlvo = n.value + 1;

  // anti-duplicata: já existe card pra este ator/nível?
  const repetido = game.messages.contents.slice(-50).some((m) => {
    const f = m.flags?.[MOD];
    return f && f.ator === actor.uuid && f.nivelAlvo === nivelAlvo;
  });
  if (repetido) return;

  const dest = game.users
    .filter((u) => u.isGM || actor.testUserPermission(u, "OWNER"))
    .map((u) => u.id);
  await ChatMessage.create({
    content: `<div class="t20tb-card">✨ <b>${esc(actor.name)}</b> tem XP para o nível ${nivelAlvo} (${n.xp.value}/${n.xp.proximo} XP). <button type="button" class="t20tb-levelup">Subir de nível</button></div>`,
    whisper: dest,
    flags: { [MOD]: { ator: actor.uuid, nivelAlvo } },
  });
}

Hooks.on("updateActor", (actor, changes) => {
  if (foundry.utils.hasProperty(changes, "system.attributes.nivel.xp.value"))
    verificarNivel(actor);
});

async function escolherClasse(classes) {
  if (classes.length === 1) return classes[0];
  const opcoes = classes
    .map((c) => `<option value="${c.id}">${esc(c.nome)} (nível ${c.niveis})</option>`)
    .join("");
  const id = await foundry.applications.api.DialogV2.prompt({
    window: { title: "Qual classe sobe de nível?" },
    content: `<select name="classe" style="width:100%">${opcoes}</select>`,
    ok: { label: "Subir", callback: (event, button) => button.form.elements.classe.value },
  });
  return classes.find((c) => c.id === id) ?? null; // dismiss resolve null (rejectClose default false)
}

async function subirNivel(message) {
  const actor = await fromUuid(message.getFlag(MOD, "ator"));
  const nivelAlvo = message.getFlag(MOD, "nivelAlvo");
  if (!actor?.isOwner) return;
  const n = actor.system.attributes.nivel;
  // revalida: clique duplo, XP editado no meio, card velho
  if (n.value >= nivelAlvo)
    return ui.notifications.info(`${actor.name} já está no nível ${n.value}.`);
  if (!podeSubir(n.value, n.xp.value, n.xp.proximo))
    return ui.notifications.warn(`${actor.name} não tem mais XP para subir (o XP mudou?).`);
  const classes = classesDe(actor.items);
  if (!classes.length)
    return ui.notifications.warn(`${actor.name} não tem nenhuma classe na ficha.`);
  const escolhida = await escolherClasse(classes);
  if (!escolhida) return;
  await actor.items.get(escolhida.id).update({ "system.niveis": escolhida.niveis + 1 });
  await ChatMessage.create({
    content: `🎉 <b>${esc(actor.name)}</b> subiu para o nível ${nivelAlvo} (${esc(escolhida.nome)} ${escolhida.niveis + 1}). PV, PM e perícias novas: ajustar na ficha.`,
  });
}

Hooks.on("renderChatMessageHTML", (message, html) => {
  const btn = html.querySelector(".t20tb-levelup");
  if (!btn) return;
  const uuid = message.getFlag(MOD, "ator");
  const nivelAlvo = message.getFlag(MOD, "nivelAlvo");
  if (!uuid) return btn.remove();
  const actor = fromUuidSync(uuid);
  if (!actor) return btn.remove();
  if ((actor.system.attributes?.nivel?.value ?? 0) >= nivelAlvo) {
    const feito = document.createElement("span");
    feito.className = "t20tb-feito";
    feito.textContent = `✔ nível ${nivelAlvo} alcançado`;
    btn.replaceWith(feito);
    return;
  }
  if (!actor.isOwner) return btn.remove();
  btn.addEventListener("click", () => subirNivel(message));
});
```

Limitação aceita (não é bug): o card não re-renderiza sozinho depois do level up — o botão velho
continua na tela até dar F5 ou rolar o chat de novo, mas o clique repetido cai na revalidação e só
avisa "já está no nível N". No reload vira "✔ nível N alcançado".

- [ ] **Step 2: Acrescentar em `styles/t20-tokenbar.css`**

```css
/* Card de subir de nível no chat */
.t20tb-card .t20tb-levelup {
  display: block;
  margin-top: 4px;
}
.t20tb-feito {
  color: #6c6;
  font-weight: bold;
}
```

- [ ] **Step 3: Verificar sintaxe + regras**

Run: `node --check "C:/Users/rayna/Soltos/t20-tokenbar/scripts/main.mjs" && node "C:/Users/rayna/Soltos/t20-tokenbar/scripts/regras.mjs" --check`
Expected: `regras.mjs OK`, exit 0.

- [ ] **Step 4: Commit**

```bash
cd "C:/Users/rayna/Soltos/t20-tokenbar" && git add scripts/main.mjs styles/t20-tokenbar.css && git -c user.name="Ray" -c user.email="raimundojmdsn@gmail.com" commit -m "feat: card de subir de nivel com +1 na classe (nivel derivado do sistema)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: README + CLAUDE.md + checklist manual no Foundry

**Files:**
- Create: `README.md`
- Modify: `C:\Users\rayna\Soltos\CLAUDE.md` (acrescentar o projeto na lista `## Projetos`, em ordem — entre `t20-inspiracao` e `tah-t20`)

**Interfaces:** nada.

- [ ] **Step 1: Criar `README.md`**

```markdown
# T20 TokenBar

Mod do [Monk's TokenBar](https://github.com/ironmonk88/monks-tokenbar) para Tormenta20 (Foundry v13).
Não substitui o monks — os dois ficam ativos juntos (`requires` no module.json).

- **Subir de nível de verdade**: XP bateu o necessário (qualquer origem) → card sussurrado com
  botão que dá +1 em `system.niveis` da classe (escolhe qual, se multiclasse). Nível/treino/CD o
  sistema recalcula sozinho; PV/PM/perícias ajusta na ficha. Respeita o setting "send level up
  whisper" do monks.
- **Inspiração** do [t20-inspiracao] na coroa da barra (✨N), com dar/tirar no botão direito (GM).
- **Stats dos jogadores**: lista separada da do mestre (menu nas configurações do módulo), com o
  stat especial `@estimate` = texto/cor do Health Estimate. A barra 1 dos jogadores vira a
  estimativa (cor + faixa, sem vazar PV exato) — desligável.
- **Modificadores no diálogo de teste** do monks (atributos e perícias).

healthEstimate e t20-inspiracao são opcionais: sem eles, as features deles só não aparecem.

Dev: repo em `C:\Users\rayna\Soltos\t20-tokenbar`, junction em
`X:\FoundryVTT\Data\modules\t20-tokenbar` — editar e F5. Testes: `node scripts/regras.mjs --check`.
```

- [ ] **Step 2: Acrescentar no `C:\Users\rayna\Soltos\CLAUDE.md`**

Inserir depois do bloco do `t20-inspiracao` (que termina em "Sem repo no GitHub (só git local).") e antes de `- `tah-t20/``:

```markdown
- `t20-tokenbar/` — Mod do Monk's TokenBar pro Tormenta20 (Foundry v13; os dois módulos ativos
  juntos, `requires` no manifest). Level up REAL: nível no T20 é derivado (soma de
  `system.niveis` dos itens classe — escrever `nivel.value` não persiste), então o card no chat
  (GM ativo cria via `updateActor`, qualquer origem de XP) tem botão que dá +1 na classe
  (DialogV2 se multiclasse); "resolvido" é derivado (`nivel >= alvo`), nunca gravado — jogador
  não pode atualizar mensagem do GM. Inspiração ✨N do t20-inspiracao na coroa da barra + botão
  direito dá/tira. Stats mestre × jogadores: lista extra (`stats-jogador`) editada por subclasse
  do EditStats do monks; pro jogador o bloco `.token-stats` é REESCRITO no `renderTokenBar` (o
  `updateEntry` lê stats por ficha antes do fallback e vazaria o PV exato por qualquer patch de
  getter). Stat `@estimate` = texto/cor do healthEstimate (`getEstimation` exige o placeable
  `.object`; espelha `breakOverlayRender`/`hideEstimate` senão vira bypass); barra 1 do jogador
  ganha cor E largura da faixa (largura real vazaria o PV). Só 2 patches de protótipo
  (`Tormenta20Rolls.getValue`/`assignXP` — save/skill caem ambos em `system.pericias`); resto é
  hook público (`renderTokenBar`, `getTokenbarContextOptionsTokenBar` — nome vem do
  `_createContextMenu` do core). healthEstimate/t20-inspiracao opcionais em runtime. Junction em
  `X:\FoundryVTT\Data\modules\t20-tokenbar` — editar e F5. Testes: `node scripts/regras.mjs
  --check`. Git local, sem GitHub.
```

- [ ] **Step 3: Commit (só o repo do módulo; CLAUDE.md fica fora de git)**

```bash
cd "C:/Users/rayna/Soltos/t20-tokenbar" && git add README.md && git -c user.name="Ray" -c user.email="raimundojmdsn@gmail.com" commit -m "docs: README

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

- [ ] **Step 4: Checklist manual no Foundry (Ray, com o mundo T20 aberto)**

O executor PARA aqui e entrega este checklist — não tem como rodar o Foundry. Ordem pensada pra
testar com 1 GM + 1 jogador (2 abas/navegadores):

1. **Boot**: ativar `t20-tokenbar` no mundo (monks-tokenbar + healthEstimate + t20-inspiracao
   ativos). Console (F12): `t20-tokenbar | init`, sem erro vermelho.
2. **getValue**: GM pede um teste (ícone da barra → perícia qualquer) — o diálogo mostra o
   modificador de cada ator ao lado do nome (antes vinha vazio).
3. **Stats dos jogadores**: Configurações → T20 TokenBar → "Stats dos jogadores" → adicionar
   `@estimate` (e p.ex. `attributes.defesa.value`) → salvar. Na aba do JOGADOR: barra mostra o
   texto do healthEstimate colorido; a barra 1 dos tokens muda de largura por faixa (ferir um NPC
   de leve NÃO mexe a barra até trocar de faixa). Na aba do GM: nada mudou.
4. **hideHealthEstimate**: marcar a flag num token (config do healthEstimate no token) → na aba
   do jogador o stat some; na do GM (se GM tivesse lista) apareceria com `*`.
5. **Inspiração**: GM dá botão direito num personagem na barra → "Dar inspiração ✨" → coroa ✨N
   aparece nas DUAS abas; tooltip `N/cap`; "Tirar inspiração" volta; cap respeitado.
6. **Level up**: GM dá XP pelo monks (botão de XP da barra) até bater `xp.proximo` → card
   sussurrado chega pro dono + GM. Na aba do jogador: clicar "Subir de nível" → (multiclasse:
   diálogo de classe) → confirmação pública "subiu para o nível N (Classe N)", ficha mostra o
   nível novo, `treino`/CD recalculados. Card antigo após F5: "✔ nível N alcançado".
7. **Anti-duplicata**: editar XP na mão pra baixo e de volta pra cima → NÃO cria segundo card do
   mesmo nível.
8. **Sem opcionais**: desativar healthEstimate e t20-inspiracao → barra continua funcionando,
   sem erro no console (@estimate e ✨ apenas somem).
```

---

## Self-review (feito na escrita)

- **Cobertura do spec**: Feature 1 → Tasks 4+5; Feature 2 → Task 5; Feature 3 → Task 6; Feature 4 → Task 3; Feature 5 → Tasks 2+7; module.json/junction → Task 1; docs → Task 8. Fora de escopo do spec (XP pós-combate, T$, assistente completo) segue fora.
- **Placeholders**: nenhum — todo step de código tem o código completo.
- **Consistência de nomes**: `MOD`, `temInspiracao`, `esc`, `estimativa`, `statHTML`, `podeSubir`, `classesDe`, `verificarNivel`, `escolherClasse`, `subirNivel`, `EditStatsJogador` — cada um definido antes (no arquivo) de onde é usado; tasks só acrescentam no fim do main.mjs, exceto Task 4 que troca o corpo do `init` (old/new dados por extenso).
